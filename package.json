{
  "name": "notation-lib",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build.js",
    "build": "node build.js --once",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.5"
  }
}
